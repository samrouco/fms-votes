import { AdditionalScoreModel } from './additional-score-model';

describe('AdditionalScoreModel', () => {
  it('should create an instance', () => {
    expect(new AdditionalScoreModel()).toBeTruthy();
  });
});
